<div class="jumbotron text-center">
  <h1>Welcome to Dev Match</h1>
  <h3>A memebership site where entrepreneurs can meet developers.</h3>
</div>

<div class="row">
  <% if user_signed_in? %>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">Your Profile</h2>
        <% if current_user.profile %>
          <%= link_to "Edit your profile", edit_user_profile_path(current_user), class: 'btn btn-default btn-lg btn-block' %>
          <%= link_to "View your profile", user_path(current_user), class: 'btn btn-default btn-lg btn-block' %>
        <% else %>
          <p>Fill out your profile so people in the community can find you.</p>
          <%= link_to "Create your profile!", new_user_profile_path(current_user), class: 'btn btn-default' if current_user %>
        <% end %>
      </div>
    </div>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">The Community</h2>
        <p>Check out the community to meet developers, entrepreneurs, and investors than can help you with your project!</p>
        <%= link_to "Visit the community.", "#", class: 'btn btn-default btn-lg btn-block' %>
      </div>
    </div>
  <% else %>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">Basic Membership</h2>
        <h4 class="text-center">Sign up for free to the community with this basic account.</h4><br>
        <%= link_to "Sign up for basic", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
      </div>
    </div>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">Pro Membership</h2>
        <h4 class="text-center">Gain access to more information with this pro account for only $10/month.</h4>
        <%= link_to "Go Pro!", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg btn-block' %>
      </div>
    </div>
  <% end %>
</div>